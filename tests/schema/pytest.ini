[pytest]
# Pytest config for schema/v1 regression tests (issue #86).
#
# Collected independently of the per-package pytest configs under ``etl/``,
# ``l3/``, and ``wire/`` — these tests have no dependency on those packages
# and only need PyYAML + jsonschema.
testpaths = .
